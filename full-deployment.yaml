# deployment

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: []
  labels: {
    key: va,
    key2: val,
  }
  name: xxx
  namespace: xxx
spec:
  replicas: 1
  selector:
    matchLabels: {

    }
  template:
    metadata:
      name: asd
      labels: {
        k8s-app: xxxxx
      }
    spec:
      restartPolicy: always
      containers:
        - name: xxx
          image: xxx
          volumeMounts:
            - mountPath: ""
              name: ""
          args:
            - xxx
        - name: fluentd-sidecar
          image: xxx
          volumeMounts:
            - mountPath: ""
              name: ""

---
apiVersion: v1
kind: Service
metadata:
  name: xxx
spec:
  type: NodePort
  ports:
    - name: export
      nodePort: 8000
      port: 8800
  selector: {
    k8s-app: xxxxx
  }